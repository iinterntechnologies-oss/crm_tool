# Frontend Environment Configuration
# 
# Update these values to match your authentication credentials
# The app will automatically register/login with these credentials on startup

# API Backend URL (change if running backend on different host/port)
VITE_API_BASE_URL=http://localhost:8000

# Demo/Default User Credentials
# These credentials will be used for automatic registration and login
# Change these to your desired username and password
VITE_DEMO_EMAIL=admin@crm.local
VITE_DEMO_PASSWORD=AdminPassword123!

# This allows the app to auto-login on startup with the above credentials
# Set to 'false' if you want to disable auto-login and require manual login
VITE_AUTO_LOGIN=true
